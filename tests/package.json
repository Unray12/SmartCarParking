{
  "name": "smartcarparking-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test suite cho SmartCarParking (frontend E2E + backend API)",
  "scripts": {
    "test": "playwright test",
    "test:backend": "playwright test --project=backend",
    "test:frontend": "playwright test --project=frontend",
    "test:headed": "playwright test --project=frontend --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
